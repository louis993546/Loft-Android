package io.github.louistasitszho.loft.common.keyValueStore

//import org.junit.Assert.*

class KeyValueStoreImplTest {
//    val store = KeyValueStoreImpl() //TODO need something to mock context

    fun `after token being store, the same token should be returned when it is being requested`() {
        assert(true)
    }
}